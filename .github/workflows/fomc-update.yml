import requests
from bs4 import BeautifulSoup
import json, csv
from datetime import datetime

# --- 核心配置 ---
TARGET_URL = "https://www.federalreserve.gov/monetarypolicy/fomccalendars.htm"
HEADERS = {
    'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36'
}
OUTPUT_JSON = "fomc_calendar.json"
OUTPUT_CSV = "fomc_calendar.csv"
OUTPUT_TXT = "fomc_dates.txt"
# -----------------

def fetch_and_parse():
    """主抓取与解析函数"""
    print(f"正在访问: {TARGET_URL}")
    try:
        resp = requests.get(TARGET_URL, headers=HEADERS, timeout=25)
        resp.raise_for_status()
    except Exception as e:
        print(f"❌ 网络请求失败: {e}")
        return None

    # --- 新增：保存网页快照用于调试 ---
    debug_html_path = "debug_fomc_page.html"
    with open(debug_html_path, "w", encoding="utf-8") as f:
        f.write(resp.text)
    print(f"💾 已将页面HTML保存至 {debug_html_path}，用于分析结构")
    # ---------------------------------

    soup = BeautifulSoup(resp.content, 'lxml')
    meetings = []

    # --- 关键：多种选择器策略，提高容错率 ---
    # 策略一：尝试查找所有包含“panel panel-default”的div（这是官网历史使用的年份板块容器）
    year_panels = soup.find_all('div', class_='panel panel-default')
    
    for panel in year_panels:
        # 在板块内找年份
        year_title = panel.find('h4', class_='panel-title')
        if not year_title:
            continue
        # 从标题文本中提取年份数字
        year_text = ''.join(filter(str.isdigit, year_title.get_text()))
        if not year_text:
            continue
        current_year = int(year_text)

        # 在板块内找会议表格
        table = panel.find('table')
        if not table:
            continue

        # 解析表格行
        for row in table.find_all('tr')[1:]:  # 跳过表头行
            cols = row.find_all('td')
            if len(cols) < 2:
                continue
            
            # 第一列是日期，第二列是会议类型
            date_cell_text = cols[0].get_text(strip=True)
            meeting_type = cols[1].get_text(strip=True)
            
            # 清洗并构造日期字符串 (例如: "January 28-29, 2025")
            # 注意：这里需要将日期字符串与年份结合，并进行更精细的解析
            # 以下是一个简单的示例，实际需根据官网日期格式调整
            raw_date_str = f"{date_cell_text}, {current_year}"
            
            # 此处简化处理，实际应解析raw_date_str为start_date和end_date
            # 您可能需要使用dateutil或手动拆分“-”来处理日期范围
            start_date = f"{current_year}-01-01"  # 此处应为解析后的真实开始日期
            end_date = f"{current-year}-01-01"    # 此处应为解析后的真实结束日期
            
            # 判断是否为SEP会议 (通常描述中会包含相关信息)
            is_sep = "summary of economic projections" in meeting_type.lower()
            
            meetings.append({
                "start_date": start_date,
                "end_date": end_date,
                "summary": meeting_type,
                "is_sep": is_sep,
                "has_press": is_sep,  # 假设SEP会议都有新闻发布会
                "source_url": TARGET_URL,
                "scraped_at": datetime.now().isoformat()
            })

    print(f"✅ 初步解析到 {len(meetings)} 个会议条目")
    return meetings

def save_data(meetings):
    """保存数据到文件"""
    if not meetings:
        print("⚠️  未获取到数据，将创建空文件。")
        meetings = []  # 确保为空列表，避免后续错误

    # 1. 保存为JSON (结构化最好)
    with open(OUTPUT_JSON, 'w', encoding='utf-8') as f:
        json.dump({
            "meta": {
                "last_updated": datetime.now().isoformat(),
                "source": TARGET_URL,
                "count": len(meetings)
            },
            "meetings": meetings
        }, f, indent=2, ensure_ascii=False)

    # 2. 保存为CSV (便于表格软件打开)
    if meetings:
        fieldnames = ["start_date", "end_date", "summary", "is_sep", "has_press"]
        with open(OUTPUT_CSV, 'w', encoding='utf-8', newline='') as f:
            writer = csv.DictWriter(f, fieldnames=fieldnames)
            writer.writeheader()
            writer.writerows(meetings)
    else:
        # 如果无数据，创建一个只有表头的CSV
        with open(OUTPUT_CSV, 'w', encoding='utf-8', newline='') as f:
            f.write("start_date,end_date,summary,is_sep,has_press")

    # 3. 保存为纯文本TXT (仅日期，每行一个)
    with open(OUTPUT_TXT, 'w', encoding='utf-8') as f:
        if meetings:
            for m in meetings:
                f.write(f"{m['start_date']} to {m['end_date']}: {m['summary']}\n")
        else:
            f.write("No meetings found.\n")

    print(f"📁 数据已保存至: {OUTPUT_JSON}, {OUTPUT_CSV}, {OUTPUT_TXT}")

# --- 脚本执行入口 ---
if __name__ == "__main__":
    print("🚀 开始执行FOMC日历抓取任务")
    data = fetch_and_parse()
    save_data(data)
    print("🎉 任务完成！")
